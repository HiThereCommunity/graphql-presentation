{
  todoItem: {
    type: GraphQLTodoItem,
    args: {
      id: {
        type: new GraphQLNonNull(GraphQLID)
      }
    },
    resolve: (
      root,
      args: {id: string},
      context
    ): Promise<?TodoItem> => TodoItem.gen(context.viewer, args.id)
  }
}
